; Set custom file for packages
(setq custom-file "~/.emacs.custom.el")

; Hide welcome screen
(setq inhibit-startup-screen t)

; Hide tool bar, menu bar and scroll bar
(menu-bar-mode 0)
(tool-bar-mode 0)
(scroll-bar-mode 0)

; Show line numbers
(global-display-line-numbers-mode)
